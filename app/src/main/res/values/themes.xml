<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NASTV" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NASTV" parent="Base.Theme.NASTV" />
    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/icon_splash1</item>
<!--        <item name="android:windowBackground">@drawable/app_icon_new</item>-->

        <!-- Optionally, customize other attributes like text color, etc. -->
    </style>
    <style name="SecondaryTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/icon_splash1</item>
<!--        <item name="android:windowBackground">@drawable/splash_tv</item>-->

        <!-- Optionally, customize other attributes like text color, etc. -->
    </style>

</resources>